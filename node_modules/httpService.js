myApp.factory('myHttp', function($http,myConfig) {
    return {
        getMsg: () => "Hello from factory",
        getToken: (user)=> $http.post(myConfig.serverUrl,user),
        getAllExhib: ()=>$http.get(myConfig.servUrl+'/api/dictionary/exhibitionCategory/all')

    };
});
